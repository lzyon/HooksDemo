import React from "react";
import { observable, isArrayLike, computed, action } from "mobx";
import { observer } from "mobx-react";
import "./City.scss";

const City = (props: any) => {
  return <div className="city-page">123</div>;
};
export default observer(City);
